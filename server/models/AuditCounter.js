const mongoose=require('mongoose');const schema=new mongoose.Schema({key:{type:String,required:true},year:{type:Number,required:true},sequence:{type:Number,default:0}});schema.index({key:1,year:1},{unique:true});module.exports=mongoose.model('AuditCounter',schema);
