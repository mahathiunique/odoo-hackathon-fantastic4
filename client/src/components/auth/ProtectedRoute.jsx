import {Navigate,useLocation} from 'react-router-dom';import useAuth from '../../hooks/useAuth';export default function ProtectedRoute({children}){const {user,ready}=useAuth(),loc=useLocation();if(!ready)return null;return user?children:<Navigate to="/login" state={{from:loc}} replace/>}
