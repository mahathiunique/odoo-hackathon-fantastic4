import {ChevronRight} from 'lucide-react';import {Link,useLocation} from 'react-router-dom';export default function Breadcrumb(){const p=useLocation().pathname.split('/').filter(Boolean);return <nav aria-label="Breadcrumb" className="mb-3 flex items-center gap-1 text-xs text-slate-500"><Link to="/dashboard">Home</Link>{p.map((x,i)=><span className="flex items-center gap-1" key={i}><ChevronRight size={12}/><span className="capitalize">{x.replaceAll('-',' ')}</span></span>)}</nav>}
