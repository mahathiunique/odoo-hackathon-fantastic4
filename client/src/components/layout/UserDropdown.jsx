import {ChevronDown,LogOut,User} from 'lucide-react';import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import useAuth from '../../hooks/useAuth';export default function UserDropdown(){const [open,setOpen]=useState(false);const {user,logout}=useAuth();const nav=useNavigate();return <div className="relative"><button onClick={()=>setOpen(!open)} className="flex items-center gap-2 rounded-lg p-1.5 hover:bg-slate-100"><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary-100 font-bold text-primary-700">{user?.name?.split(' ').map(x=>x[0]).slice(0,2)}</span><span className="hidden text-left sm:block"><b className="block text-xs text-slate-800">{user?.name}</b><small className="text-slate-500">{user?.role}</small></span><ChevronDown size={14}/></button>{open&&<div className="absolute right-0 top-12 z-30 w-48 rounded-xl border bg-white p-2 shadow-xl"><Link to="/profile" onClick={()=>setOpen(false)} className="flex gap-2 rounded-lg p-2 text-sm hover:bg-slate-50"><User size={17}/> Profile</Link><button className="flex w-full gap-2 rounded-lg p-2 text-sm text-red-600 hover:bg-red-50" onClick={()=>{logout();nav('/login')}}><LogOut size={17}/> Sign out</button></div>}</div>}
