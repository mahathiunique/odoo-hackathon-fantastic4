import {Bell} from 'lucide-react';import {useState} from 'react';import {Link} from 'react-router-dom';import useNotifications from '../../hooks/useNotifications';
export default function NotificationBell(){const [open,setOpen]=useState(false);const {items,unread,markRead}=useNotifications();return <div className="relative"><button aria-label="Notifications" className="relative rounded-lg p-2 hover:bg-slate-100" onClick={()=>setOpen(!open)}><Bell size={20}/>{unread>0&&<span className="absolute right-0 top-0 grid h-4 min-w-4 place-items-center rounded-full bg-red-500 px-1 text-[10px] text-white">{unread}</span>}</button>{open&&<div className="absolute right-0 top-12 z-30 w-[min(22rem,calc(100vw-2rem))] overflow-hidden rounded-xl border bg-white shadow-xl"><div className="border-b p-4 font-semibold text-slate-900">Notifications</div>{items.slice(0,4).map(x=><button key={x.id} onClick={()=>markRead(x.id)} className={`block w-full border-b p-4 text-left text-sm ${!x.read?'bg-indigo-50/60':''}`}><b>{x.title}</b><p className="mt-1 text-xs text-slate-500">{x.message}</p></button>)}<Link to="/notifications" onClick={()=>setOpen(false)} className="block p-3 text-center text-sm font-semibold text-primary-600">View all notifications</Link></div>}</div>}
