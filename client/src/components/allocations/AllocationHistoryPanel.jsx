import AllocationStatusBadge from './AllocationStatusBadge';export default function AllocationHistoryPanel({allocation}){const events=[['Allocated',allocation.allocatedDate,allocation.allocatedBy?.name],allocation.actualReturnDate&&['Returned',allocation.actualReturnDate,allocation.returnedBy?.name]].filter(Boolean);return <section className="card"><h2 className="font-semibold text-slate-900">Allocation history</h2><div className="mt-5 border-l-2 border-indigo-100 pl-5">{events.map(([label,date,person])=><div className="relative mb-6" key={label}><span className="absolute -left-[1.65rem] top-1 h-3 w-3 rounded-full bg-primary-500 ring-4 ring-white"/><AllocationStatusBadge status={label==='Allocated'?allocation.status:'Returned'}/><p className="mt-2 text-sm">{new Date(date).toLocaleString()}</p><p className="text-xs text-slate-500">By {person||'—'}</p></div>)}</div></section>}
