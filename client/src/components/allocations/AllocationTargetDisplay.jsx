export default function AllocationTargetDisplay({allocation}){const target=allocation.allocatedToType==='Employee'?allocation.employee:allocation.department;return <div><p className="font-medium text-slate-800">{target?.name||'—'}</p><p className="text-xs text-slate-500">{allocation.allocatedToType==='Employee'?(target?.employeeId||target?.email):(target?.code||'Department')}</p></div>}
