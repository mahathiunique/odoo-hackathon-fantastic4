import EmptyState from '../common/EmptyState';import LoadingSkeleton from '../common/LoadingSkeleton';
export default function DataTable({columns,data=[],loading=false,emptyTitle}){if(loading)return <LoadingSkeleton/>;if(!data.length)return <EmptyState title={emptyTitle||'No records found'}/>;return <div className="overflow-hidden rounded-xl border bg-white shadow-card"><div className="overflow-x-auto"><table className="w-full"><thead className="table-head"><tr>{columns.map(c=><th className="table-cell" key={c.key}>{c.label}</th>)}</tr></thead><tbody className="divide-y">{data.map((row,i)=><tr key={row.id||i} className="hover:bg-slate-50/70">{columns.map(c=><td className="table-cell" key={c.key}>{c.render?c.render(row[c.key],row):row[c.key]??'—'}</td>)}</tr>)}</tbody></table></div></div>}
