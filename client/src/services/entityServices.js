import {service} from './store';
export const departmentService=service('departments'); export const categoryService=service('categories'); export const employeeService=service('employees'); export const assetService=service('assets'); export const allocationService=service('allocations'); export const resourceService=service('resources'); export const bookingService=service('bookings'); export const maintenanceService=service('maintenance'); export const auditService=service('audits'); export const notificationService=service('notifications'); export const userService=service('users');
