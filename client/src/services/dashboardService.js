import {dashboard,assets,allocations,bookings,maintenance,audits} from '../mock/data';import {delay} from './store';export const dashboardService={async getOverview(){await delay();return{success:true,data:{...dashboard,assets:assets.slice(0,5),allocations:allocations.filter(x=>x.status==='Overdue'),bookings:bookings.slice(0,4),maintenance:maintenance.slice(0,4),audits,recent:['Laptop AF-2011 allocated to Neha Iyer','Conference Room 32 booking confirmed','MR-2026-107 marked completed','Audit discrepancy logged for AF-2016']}}}};
