import {useEffect,useState} from 'react';import {useForm} from 'react-hook-form';import {useNavigate,useParams} from 'react-router-dom';import toast from 'react-hot-toast';import PageHeader from '../components/layout/PageHeader';import Input from '../components/common/Input';import Select from '../components/common/Select';import TextArea from '../components/common/TextArea';import Button from '../components/common/Button';
export default function ModuleFormPage({title,base,service,fields}){const {id}=useParams(),nav=useNavigate(),[remoteOptions,setRemoteOptions]=useState({});const {register,handleSubmit,reset,formState:{errors,isSubmitting}}=useForm();useEffect(()=>{if(id)service.getById(id).then(r=>reset(r.data))},[id,service,reset]);useEffect(()=>{fields.filter(f=>f.loadOptions).forEach(f=>f.loadOptions().then(options=>setRemoteOptions(current=>({...current,[f.name]:options}))).catch(()=>toast.error(`Could not load ${f.label.toLowerCase()} options`)))},[fields]);const submit=async d=>{try{id?await service.update(id,d):await service.create(d);toast.success(`${title} saved successfully`);nav(base)}catch{toast.error('Could not save changes')}};return <><PageHeader title={`${id?'Edit':'Add'} ${title}`} description="Fields marked with an asterisk are required."/><form onSubmit={handleSubmit(submit)} className="card max-w-4xl"><div className="grid gap-5 md:grid-cols-2">{fields.map(f=>{const p={...register(f.name,{required:f.required&&`${f.label} is required`,pattern:f.pattern}),label:f.label,required:f.required,error:errors[f.name],placeholder:f.placeholder};if(f.type==='select')return <Select key={f.name} {...p} options={remoteOptions[f.name]||f.options}/>;if(f.type==='textarea')return <div className="md:col-span-2" key={f.name}><TextArea {...p}/></div>;return <Input key={f.name} {...p} type={f.type||'text'}/>})}</div><div className="mt-7 flex justify-end gap-3 border-t pt-5"><button type="button" onClick={()=>nav(base)} className="btn-secondary">Cancel</button><Button loading={isSubmitting}>Save {title}</Button></div></form></>}
