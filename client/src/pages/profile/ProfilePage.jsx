import {useForm} from 'react-hook-form';import toast from 'react-hot-toast';import useAuth from '../../hooks/useAuth';import PageHeader from '../../components/layout/PageHeader';import Input from '../../components/common/Input';export default function ProfilePage(){const {user}=useAuth();const {register,handleSubmit}=useForm({defaultValues:user});return <><PageHeader title="My profile" description="Manage your personal information and account preferences."/><div className="grid gap-5 lg:grid-cols-3"><aside className="card text-center"><span className="mx-auto grid h-24 w-24 place-items-center rounded-2xl bg-primary-100 text-2xl font-bold text-primary-700">{user.name.split(' ').map(x=>x[0]).join('')}</span><h2 className="mt-4 text-lg font-bold text-slate-900">{user.name}</h2><p className="text-sm text-slate-500">{user.role}</p><span className="mt-4 inline-block rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">Active account</span></aside><form className="card lg:col-span-2" onSubmit={handleSubmit(()=>toast.success('Profile updated locally'))}><h2 className="mb-5 font-semibold text-slate-900">Personal information</h2><div className="grid gap-5 sm:grid-cols-2"><Input label="Full name" {...register('name')}/><Input label="Email" disabled {...register('email')}/><Input label="Department" {...register('department')}/><Input label="Phone" {...register('phone')}/></div><button className="btn-primary mt-6">Save changes</button><div className="mt-8 border-t pt-5"><h3 className="font-semibold text-slate-800">Password</h3><p className="mt-1 text-sm text-slate-500">Password changes will be available when secure backend authentication is connected.</p><button disabled className="btn-secondary mt-3">Change password · Coming soon</button></div></form></div></>}
